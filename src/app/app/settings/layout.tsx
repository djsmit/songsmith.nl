export const metadata = {
  title: "Settings - Songsmith",
  description: "Manage your Songsmith account and profile.",
};

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
